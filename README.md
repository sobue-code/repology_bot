# ALT Repology Bot

Telegram бот для мониторинга outdated пакетов в ALT Linux через Repology и RDB.

## Возможности

- 🔍 **Проверка пакетов** - просмотр актуальности пакетов для указанных email'ов мантейнеров
- 🔴 **Интеграция с Repology** - глобальная база пакетов
- 🔴 **Интеграция с ALT RDB** - специфичная информация для ALT Linux
- 🏷️ **Реальные имена пакетов** - отображение имён пакетов в ALT (python3-module-*)
- 🔗 **Ссылки на packages.altlinux.org** - прямые ссылки на пакеты
- 🔔 **Автоматические уведомления** - настройка периодических проверок (ежедневно или еженедельно)
- 📊 **Статистика** - просмотр статистики по пакетам (количество outdated, newest и т.д.)
- 📄 **Пагинация** - удобная навигация по большим спискам пакетов
- 💾 **Кэширование** - результаты кэшируются для ускорения повторных запросов (6 часов)
- 👥 **Мультипользовательский** - поддержка нескольких пользователей с разными email'ами

## Требования

- Python 3.12+
- [uv](https://github.com/astral-sh/uv) для управления зависимостями

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd repology_bot
```

2. Установите зависимости с помощью `uv`:
```bash
uv sync
```

3. Создайте конфигурационный файл:
```bash
cp config.toml.example config.toml
```

4. Отредактируйте `config.toml`:
   - Получите токен бота от [@BotFather](https://t.me/BotFather)
   - Добавьте свой Telegram ID (можно узнать у [@userinfobot](https://t.me/userinfobot))
   - Укажите email'ы мантейнера для отслеживания

## Конфигурация

Пример `config.toml`:

```toml
[bot]
token = "YOUR_BOT_TOKEN_HERE"
admin_ids = [123456789]

[database]
path = "data/bot.db"

[logging]
level = "INFO"
file = "logs/bot.log"
max_bytes = 10485760
backup_count = 5

[repology]
api_base_url = "https://repology.org/api/v1"
request_timeout = 30
rate_limit_delay = 1.0
cache_duration = 6

[notifications]
default_check_time = "09:00"
max_packages_per_message = 20
min_notification_interval = 1

[[users]]
name = "Your Name"
telegram_id = 123456789
emails = ["maintainer@example.com"]
enabled = true
```

### Параметры конфигурации

#### `[bot]`
- `token` - токен Telegram бота
- `admin_ids` - список Telegram ID администраторов (для уведомлений об ошибках)

#### `[database]`
- `path` - путь к файлу SQLite базы данных

#### `[logging]`
- `level` - уровень логирования (`DEBUG`, `INFO`, `WARNING`, `ERROR`)
- `file` - путь к файлу логов
- `max_bytes` - максимальный размер файла логов в байтах
- `backup_count` - количество резервных копий логов

#### `[repology]`
- `api_base_url` - базовый URL API Repology
- `request_timeout` - таймаут запросов в секундах
- `rate_limit_delay` - задержка между запросами (секунды)
- `cache_duration` - длительность кэширования результатов (часы)

#### `[notifications]`
- `default_check_time` - время по умолчанию для проверок (формат `HH:MM`)
- `max_packages_per_message` - максимум пакетов в одном сообщении
- `min_notification_interval` - минимальный интервал между уведомлениями (часы)

#### `[[users]]`
- `name` - имя пользователя (для удобства)
- `telegram_id` - Telegram ID пользователя
- `emails` - список email'ов мантейнера для отслеживания
- `enabled` - включен ли пользователь (`true`/`false`)

## Запуск

### Разовый запуск

```bash
uv run python main.py
```

### Установка как systemd сервис

Используйте автоматический установщик:

```bash
./install-service.sh
```

Или следуйте инструкциям в [SYSTEMD.md](SYSTEMD.md)

## Команды бота

- `/start` - Главное меню с кнопками
- `/help` - Справка по командам
- `/check` - Проверить пакеты вручную
- `/status` - Показать мои настройки и email'ы
- `/stats` - Статистика по пакетам
- `/subscribe` - Настроить автоматические уведомления
- `/unsubscribe` - Отключить автоматические уведомления
- `/settings` - Посмотреть текущие настройки подписки

## Использование

1. **Проверка пакетов вручную**:
   - Отправьте `/check` или нажмите кнопку "🔍 Проверить пакеты"
   - Выберите email из списка
   - Выберите тип проверки (только outdated, все пакеты, обновить кэш)

2. **Настройка автоматических уведомлений**:
   - Отправьте `/subscribe` или нажмите "🔔 Настроить уведомления"
   - Выберите частоту (ежедневно или еженедельно)
   - Выберите время
   - Для еженедельных - выберите день недели

3. **Просмотр статистики**:
   - Отправьте `/stats` или нажмите "📊 Статистика"
   - Выберите email для просмотра статистики

## Структура проекта

```
repology_bot/
├── bot/                    # Telegram бот
│   ├── handlers.py         # Обработчики команд
│   ├── keyboards.py        # Inline клавиатуры
│   ├── middleware.py       # Middleware (авторизация)
│   └── subscription_handlers.py  # Обработчики подписок
├── core/                   # Ядро приложения
│   ├── config.py           # Конфигурация
│   ├── database.py         # Работа с БД
│   ├── logger.py           # Логирование
│   └── scheduler.py        # Планировщик задач
├── models/                 # Модели данных
│   ├── package.py          # Модели пакетов
│   └── user.py             # Модели пользователей
├── services/               # Сервисы
│   ├── repology.py         # API клиент Repology
│   ├── package_checker.py  # Проверка пакетов с кэшем
│   └── notification.py     # Отправка уведомлений
├── utils/                  # Утилиты
│   └── formatting.py       # Форматирование сообщений
├── data/                   # База данных (создается автоматически)
├── logs/                   # Логи (создается автоматически)
├── main.py                 # Точка входа
├── config.toml.example     # Пример конфигурации
├── pyproject.toml          # Зависимости проекта
└── README.md               # Эта документация
```

## База данных

Используется SQLite с следующими таблицами:

- `users` - пользователи из конфигурации
- `user_emails` - email'ы для отслеживания
- `subscriptions` - подписки на уведомления
- `package_cache` - кэш информации о пакетах
- `notification_history` - история отправленных уведомлений

## Логирование

Логи сохраняются в файл, указанный в конфигурации (по умолчанию `logs/bot.log`).

Уровни логирования:
- `DEBUG` - детальная отладочная информация
- `INFO` - общая информация о работе бота
- `WARNING` - предупреждения
- `ERROR` - ошибки

## Разработка

### Добавление новых команд

1. Добавьте обработчик в `bot/handlers.py` или создайте новый файл
2. Зарегистрируйте router в `main.py`
3. При необходимости добавьте новые клавиатуры в `bot/keyboards.py`

### Интеграция с ALT RDB

В будущем планируется добавить интеграцию с ALT Linux RDB API.
Для этого создайте файл `services/rdb.py` по аналогии с `services/repology.py`.

## Troubleshooting

### Бот не отвечает
- Проверьте правильность токена в `config.toml`
- Убедитесь, что ваш `telegram_id` добавлен в конфигурацию
- Проверьте логи в `logs/bot.log`

### Ошибки при проверке пакетов
- Проверьте доступность API Repology
- Увеличьте `rate_limit_delay` в конфигурации
- Проверьте правильность email'ов мантейнера

### Не приходят уведомления
- Убедитесь, что подписка активна (`/status`)
- Проверьте логи планировщика
- Убедитесь, что бот запущен в нужное время

## Лицензия

MIT

## Автор

[Your Name]
